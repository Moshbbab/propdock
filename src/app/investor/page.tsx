import { PitchCarusel } from "@/components/investor/pitch-carousel"
import { constructMetadata } from "@/lib/utils"

export const metadata = constructMetadata({
  title: "Propdock | Investor",
  description:
    "Ønsker du å investere i Propdock? Se vår investorpresentasjon for å lære mer om vår forretningsmodell og vekstrategier",
})

export default PitchCarusel
